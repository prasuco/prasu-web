---
const Links = [
  {
    name: "Home",
    href: "#",
  },
];
---

<nav>
  <ul>
    {
      Links.map((link) => (
        <li>
          <a href={link.href}>{link.name}</a>
        </li>
      ))
    }
  </ul>
</nav>
